<h1> Your pets 🐾</h1>
<br>
<div class="row">
  <div class="card" style="width: 18rem;">
    <% if @pet.photo.attached? %>
      <%= cl_image_tag @pet.photo.key, class: "card-img-top" %>
    <% else %>
      <i class="fa-solid fa-paw card-img-top"></i>
    <% end %>

    <div class="card-body">
      <h4 class="card-title"><%= @pet.name %></h4>
      <h6 class="card-subtitle"><%= @pet.breed %></h6>
      <br>
      <p class="card-text"><%= @pet.important_note %></p>
      <br>
      <%= button_to 'Delete pet', pet_path(@pet), method: :delete, class: "btn btn-sm btn-warning" %>
      <%= link_to 'Edit pet', edit_pet_path(@pet), class: 'btn btn-sm btn-info' %>
    </div>
  </div>
</div>
<br>
<%= link_to 'Back', pets_path, class: 'btn btn-info' %>
